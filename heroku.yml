build:
  docker:
    web: atelier_ideal/Dockerfile.heroku
run:
  web: gunicorn atelier_ideal.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: web
  command:
    - python manage.py collectstatic --noinput